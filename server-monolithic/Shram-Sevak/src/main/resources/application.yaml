spring:
 datasource:
  url: jdbc:mysql://localhost:3306/shram_sevak_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
  username: root
  password: root
 jpa:
  show-sql: true
  hibernate:
   ddl-auto: update
  properties:
   hibernate:
    dialect: org.hibernate.dialect.MySQL8Dialect
  open-in-view: false

 servlet:
  multipart:
   max-file-size: 10MB
   max-request-size: 11MB
   
file:
 upload:
  location: Images/Workers/
  
  file:
 upload2:
  location: Images/Customers/

  