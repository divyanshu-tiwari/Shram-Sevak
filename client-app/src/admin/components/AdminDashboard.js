// wrote the code to test whether credentials where getting stored in localStorage or not
// import { useSelector, useDispatch } from "react-redux";
// import { setCurrentUser, clearCurrentUser, selectUser } from "../../store/user/userSlice";
// import { getUser } from "../../service/base.service";

export default function AdminDashboard(){
//     const dispatch = useDispatch()
//     const user = useSelector(selectUser)

    return(
//         <>
//             <button onClick={()=>dispatch(setCurrentUser({userName:"admin", role:"admin"}))}>Set User</button>
//             <br/>
//             <button onClick={()=>getUser(user)}>Show Current User</button>
//             <br/>
//             <button onClick={()=>dispatch(clearCurrentUser())}>Clear Current User</button>
//         </>
        <>
            <h1> This is the admin dahsboard</h1>
        </>
    )
}